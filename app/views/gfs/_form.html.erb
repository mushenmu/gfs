<%= form_with(model: gf, local: true) do |form| %>
  <% if gf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gf.errors.count, "error") %> prohibited this gf from being saved:</h2>

      <ul>
      <% gf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.number_field :state %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
